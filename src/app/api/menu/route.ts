import { NextResponse } from 'next/server';import { supabaseServer } from '@/lib/supabaseClient';
export async function GET(){try{const sb=supabaseServer();const {data:cats}=await sb.from('categories').select('*').order('sort_order',{ascending:true});const {data:items}=await sb.from('menu_items').select('*');return NextResponse.json({categories:cats??[],items:items??[]})}catch{return NextResponse.json({categories:[{id:1,name:'Кафета',sort_order:0}],items:[{id:1,category_id:1,name:'Еспресо',description:'Двойно',price_cents:250,is_available:true}]})}}
